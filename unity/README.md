# Unity Loader

This folder contains placeholder scripts for loading a generated 3D model into a Unity scene. Attach `ModelLoader` to a GameObject and set `modelUrl` to the backend endpoint serving the `.glb` file.
